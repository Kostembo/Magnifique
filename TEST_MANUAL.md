# Magnifique — Руководство по ручному тестированию v0.6

## Тест-аккаунты (из seed + созданные вручную)

| Роль      | Телефон          | Пароль   | Создаётся           |
|-----------|------------------|----------|---------------------|
| admin     | +7 900 000-00-00 | admin000 | автоматически (seed)|
| owner     | +7 900 111-11-11 | 111111   | автоматически (seed)|
| owner 2   | +7 900 222-22-22 | 222222   | автоматически (seed)|
| manager   | создать вручную  | —        | admin → /employees  |
| sales     | создать вручную  | —        | admin → /employees  |
| chef      | создать вручную  | —        | admin → /employees  |
| waiter    | создать вручную  | —        | admin → /employees  |
| cook      | создать вручную  | —        | admin → /employees  |
| warehouse | создать вручную  | —        | admin → /employees  |

> Автоматические тесты: `node test-cycle.mjs` (все 79 сценариев).

---

## Блок 1 — Авторизация

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1.1 | Открыть `/login` без сессии | Страница входа |
| 1.2 | Ввести неверный пароль | Ошибка «Неверный телефон или пароль» |
| 1.3 | Войти как admin `900-000-0000 / admin000` | Редирект на `/` |
| 1.4 | Открыть `/login` уже залогиненным | Редирект на `/` |
| 1.5 | Нажать «Выйти» | Редирект на `/login` |

---

## Блок 2 — Матрица доступа к страницам

### 2A — Привилегированные роли (manager / owner / admin)

| # | Роль    | Страница        | Ожидается   |
|---|---------|-----------------|-------------|
| 2.1 | admin | `/employees`    | Открывается |
| 2.2 | admin | `/events`       | Открывается |
| 2.3 | admin | `/requisitions` | Открывается |
| 2.4 | admin | `/timesheet`    | Открывается |
| 2.5 | admin | `/calendar`     | Открывается |
| 2.6 | admin | `/menu-items`   | Открывается |
| 2.7 | admin | `/warehouse-items` | Открывается |
| 2.8 | owner | `/employees`    | Открывается |
| 2.9 | owner | `/events`       | Открывается |

### 2B — Роль sales (новая)

| # | Роль  | Страница        | Ожидается                  |
|---|-------|-----------------|----------------------------|
| 2.10 | sales | `/events`       | Открывается — видит все мероприятия |
| 2.11 | sales | `/calendar`     | Открывается — сетка мероприятий |
| 2.12 | sales | `/employees`    | Редирект на `/`            |
| 2.13 | sales | `/requisitions` | Редирект на `/`            |
| 2.14 | sales | `/menu-items`   | Редирект на `/`            |

### 2C — Роль chef (новая)

| # | Роль | Страница     | Ожидается                  |
|---|------|--------------|----------------------------|
| 2.15 | chef | `/events`    | Открывается — видит все мероприятия |
| 2.16 | chef | `/calendar`  | Открывается — сетка мероприятий |
| 2.17 | chef | `/employees` | Редирект на `/`            |

### 2D — Ограниченные роли

| # | Роль      | Страница        | Ожидается                      |
|---|-----------|-----------------|-------------------------------|
| 2.18 | waiter  | `/employees`    | Редирект на `/`               |
| 2.19 | waiter  | `/events`       | Открывается (только свои)     |
| 2.20 | warehouse | `/events`     | Редирект на `/`               |
| 2.21 | warehouse | `/requisitions`| Открывается                  |

---

## Блок 3 — Управление сотрудниками (admin / owner / manager)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 3.1 | Войти как admin → открыть `/employees` | Список всех сотрудников |
| 3.2 | Нажать «Добавить» → роль `sales` | Роль отображается как «Менеджер по продажам» |
| 3.3 | Добавить сотрудника с ролью `chef` | Роль «Шеф-повар» |
| 3.4 | Добавить с ролью `owner` | Роль «Владелец» |
| 3.5 | Добавить с ролью `admin` | Роль «Администратор» |
| 3.6 | Редактировать → сменить роль `waiter` → `sales` | Роль сохраняется |
| 3.7 | Сбросить пароль (≥6 символов) | Toast «Пароль сброшен», вход с новым паролем работает |
| 3.8 | Деактивировать сотрудника | Карточка становится прозрачной, фильтр «Активные» скрывает |
| 3.9 | Попытка войти как `waiter` → `/employees` | Редирект на `/` |
| 3.10 | Попытка войти как `sales` → `/employees` | Редирект на `/` |

---

## Блок 4 — Каталог меню (`/menu-items`)

> Доступ: только `manager`, `owner`, `admin`. Sales и ниже — нет доступа.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 4.1 | Войти как manager → `/menu-items` | Страница «Каталог меню» |
| 4.2 | Войти как sales → `/menu-items` | Редирект на `/` |
| 4.3 | Добавить «Цезарь», категория «Салаты», ед. «порц» | Строка в таблице |
| 4.4 | Добавить «Говядина в соусе», «Горячее», «порц» | Добавляется |
| 4.5 | Добавить «Тирамису», «Десерты», «порц» | Добавляется |
| 4.6 | Удалить строку | Исчезает (soft delete, в API `is_active=false`) |
| 4.7 | `POST /api/menu-items` от `waiter` (Postman/curl) | 403 |

---

## Блок 5 — Каталог склада (`/warehouse-items`)

> Доступ: только `manager`, `owner`, `admin`.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 5.1 | Войти как manager → `/warehouse-items` | Страница «Каталог склада» |
| 5.2 | Добавить «Скатерти белые», «Текстиль», «шт» | Добавляется |
| 5.3 | Добавить «Бокалы», «Посуда», «шт» | Добавляется |
| 5.4 | Добавить «Вино красное», «Напитки», «бут» | Добавляется |
| 5.5 | Удалить строку | Исчезает (soft delete) |

---

## Блок 6 — Создание мероприятия + Push-уведомления

> **Новое:** events создаёт не только manager, но и `sales`. Push уходит manager + owner + admin.

| # | Роль | Действие | Ожидаемый результат |
|---|------|----------|---------------------|
| 6.1 | sales | `/events` → нажать «Создать» | Форма открывается |
| 6.2 | sales | Заполнить поля → «Сохранить» | Мероприятие создано, статус `recruiting` |
| 6.3 | manager | Получить push | Уведомление «Новое мероприятие: [название]» |
| 6.4 | owner | Получить push | Аналогичное уведомление |
| 6.5 | admin | Получить push | Аналогичное уведомление |
| 6.6 | chef | `POST /api/events` | 403 (chef не создаёт события) |
| 6.7 | waiter | `POST /api/events` | 403 |
| 6.8 | warehouse | `POST /api/events` | 403 |

---

## Блок 7 — Вкладка «Меню» в мероприятии

> **Новый механизм:** вкладка «Меню» с autocomplete из каталога `/menu-items`.

### 7A — Видимость вкладки по ролям

| # | Роль    | Вкладка «Меню» | Редактирование |
|---|---------|----------------|----------------|
| 7.1 | sales   | Видна          | Да             |
| 7.2 | manager | Видна          | Да             |
| 7.3 | owner   | Видна          | Да             |
| 7.4 | admin   | Видна          | Да             |
| 7.5 | chef    | Видна          | Нет (только просмотр) |
| 7.6 | waiter  | Не видна       | —              |
| 7.7 | cook    | Не видна       | —              |

### 7B — Autocomplete и сохранение меню

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 7.8 | Войти как sales → открыть мероприятие → вкладка «Меню» | Форма добавления видна |
| 7.9 | Ввести «Цез» (≥2 символов) | Дропдаун: «Цезарь · порц · Салаты» |
| 7.10 | Выбрать из дропдауна | Поле названия + ед. заполнились автоматически |
| 7.11 | Ввести «xyz» | Дропдаун не появляется |
| 7.12 | Добавить 3 блюда → «Сохранить меню» | Toast «Меню сохранено» |
| 7.13 | Обновить страницу → вкладка «Меню» | Все 3 блюда сохранились |
| 7.14 | Войти как chef → то же мероприятие | Вкладка «Меню» видна, кнопок добавления/удаления нет |
| 7.15 | Войти как manager → то же мероприятие | Вкладка «Меню» видна, редактирование доступно |

---

## Блок 8 — Набор персонала

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 8.1 | Менеджер нажимает «Позвать костяк» | Toast «Приглашено N чел.», push ушли |
| 8.2 | Waiter получил push → открывает приглашение | Кнопки «Принять» / «Отказаться» |
| 8.3 | Waiter нажимает «Принять» | Статус → confirmed, видно в наборе |
| 8.4 | Менеджер нажимает «Открыть пул» | Приглашения уходят regular-сотрудникам |
| 8.5 | Менеджер нажимает «Напомнить молчунам» | Toast «Напоминание отправлено N чел.» |
| 8.6 | Менеджер удаляет сотрудника из набора (×) | Строка исчезает, счётчик уменьшается |
| 8.7 | Менеджер нажимает «Экспорт PDF» | Скачивается PDF с кириллицей |

---

## Блок 9 — Складская заявка + Autocomplete

> **Новый механизм:** при добавлении позиции в заявку — autocomplete из `/api/warehouse-items`.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 9.1 | Менеджер создаёт заявку в мероприятии | Статус `draft` |
| 9.2 | Открыть заявку → поле «Название» позиции | Форма добавления |
| 9.3 | Ввести «Скат» (≥2 символов) | Дропдаун: «Скатерти белые · шт · Текстиль» |
| 9.4 | Выбрать из дропдауна | Название + ед. заполнились автоматически |
| 9.5 | Ввести «xyz» — нет совпадений | Дропдаун не появляется |
| 9.6 | Добавить позицию вручную (без autocomplete) | Строка в списке заявки |
| 9.7 | Менеджер нажимает «Отправить на склад» | Статус → `sent` |
| 9.8 | Склад открывает заявку | Кнопка «Начать сборку» |
| 9.9 | Склад отмечает позиции ✓ → «Завершить» | Статус → `done`, прогресс 100% |

---

## Блок 10 — Табель

| # | Роль    | Действие | Ожидаемый результат |
|---|---------|----------|---------------------|
| 10.1 | waiter  | `/timesheet` | Видит только свою строку |
| 10.2 | waiter  | Вносит время 18:00–23:00 | Запись сохраняется |
| 10.3 | manager | `/timesheet` | Видит всех сотрудников |
| 10.4 | manager | Навести на ячейку с записью | Иконка карандаша |
| 10.5 | manager | Кликнуть → диалог | Поля start/end time |
| 10.6 | manager | Изменить время → сохранить | Ячейка обновилась |
| 10.7 | manager | Удалить запись | Ячейка очистилась |
| 10.8 | owner   | `/timesheet` | Видит всех (как manager) |

---

## Блок 11 — Календарь

| # | Роль                        | Ожидаемый результат                        |
|---|-----------------------------|--------------------------------------------|
| 11.1 | manager / owner / admin  | Сетка всех активных мероприятий            |
| 11.2 | **sales** (новое)         | Сетка всех активных мероприятий            |
| 11.3 | **chef** (новое)          | Сетка всех активных мероприятий            |
| 11.4 | waiter / cook              | Личный календарь подтверждённых смен       |
| 11.5 | Клик на мероприятие        | Переход на `/events/[id]`                  |

---

## Блок 12 — Матрица доступа API (curl / Postman)

| # | Запрос | Ожидается |
|---|--------|-----------|
| 12.1 | `GET /api/employees` без куки | 401 |
| 12.2 | `GET /api/employees` от `waiter` | 403 |
| 12.3 | `POST /api/employees` от `waiter` | 403 |
| 12.4 | `GET /api/requisitions` от `waiter` | 403 |
| 12.5 | `POST /api/events` от `warehouse` | 403 |
| 12.6 | `POST /api/events` от `chef` | 403 |
| 12.7 | `POST /api/events` от `cook` | 403 |
| 12.8 | **`POST /api/events` от `sales`** | 201 ✅ (разрешено) |
| 12.9 | `POST /api/menu-items` от `waiter` | 403 |
| 12.10 | `POST /api/menu-items` от `sales` | 403 (sales не управляет каталогом) |
| 12.11 | `POST /api/menu-items` от `manager` | 201 ✅ |
| 12.12 | `GET /api/employees` от `admin` | 200 ✅ |
| 12.13 | `GET /api/employees` от `owner` | 200 ✅ |
| 12.14 | `GET /api/events/[id]/menu` от `chef` | 200 ✅ (только чтение) |
| 12.15 | `POST /api/events/[id]/menu` от `chef` | 403 (chef не редактирует меню) |

---

## Автоматический прогон

```bash
# Запустить dev-сервер
npm run dev

# В другом терминале
node test-cycle.mjs
# Ожидаемый результат: ✅ ВСЕ 79 ТЕСТОВ ПРОШЛИ
```
